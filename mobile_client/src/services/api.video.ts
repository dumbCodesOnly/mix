/**\n * Video generation API client methods.\n *\n * This module extends the API client with video generation capabilities:\n * - Text-to-video generation\n * - Image-to-video generation\n */\n\nimport axios, { AxiosInstance } from 'axios';\n\nexport interface TextToVideoParams {\n  prompt: string;\n  model?: string;\n  negative_prompt?: string;\n  duration?: number;\n  fps?: number;\n  num_inference_steps?: number;\n}\n\nexport interface ImageToVideoParams {\n  image: string; // Base64 encoded image\n  model?: string;\n  prompt?: string;\n  duration?: number;\n  fps?: number;\n  num_inference_steps?: number;\n}\n\nexport interface VideoGenerationResponse {\n  video_url: string;\n  video_size: number;\n  duration: number;\n  fps: number;\n  resolution: string;\n  model: string;\n  generation_time: number;\n  format: string;\n}\n\n/**\n * Add video generation methods to API client.\n *\n * Usage:\n * ```typescript\n * const apiClient = getAPIClient();\n * const videoBlob = await apiClient.generateTextToVideo({\n *   prompt: \"A cat walking through a garden\",\n *   duration: 8,\n *   fps: 24,\n * });\n * ```\n */\nexport const addVideoMethods = (client: AxiosInstance) => {\n  /**\n   * Generate video from text prompt.\n   *\n   * @param params - Text-to-video generation parameters\n   * @returns Promise<Blob> - Generated video as MP4 blob\n   *\n   * @throws Error if generation fails\n   *\n   * @example\n   * ```typescript\n   * const videoBlob = await apiClient.generateTextToVideo({\n   *   prompt: \"A beautiful sunset over mountains\",\n   *   duration: 8,\n   *   fps: 24,\n   *   num_inference_steps: 50,\n   * });\n   * ```\n   */\n  client.generateTextToVideo = async (\n    params: TextToVideoParams\n  ): Promise<Blob> => {\n    try {\n      const response = await client.post('/api/video/text-to-video', params, {\n        responseType: 'blob',\n        timeout: 600000, // 10 minutes for long-running generation\n      });\n\n      return response.data as Blob;\n    } catch (error) {\n      if (axios.isAxiosError(error)) {\n        throw new Error(\n          error.response?.data?.message || 'Failed to generate text-to-video'\n        );\n      }\n      throw error;\n    }\n  };\n\n  /**\n   * Generate video from image.\n   *\n   * @param params - Image-to-video generation parameters\n   * @returns Promise<Blob> - Generated video as MP4 blob\n   *\n   * @throws Error if generation fails\n   *\n   * @example\n   * ```typescript\n   * const videoBlob = await apiClient.generateImageToVideo({\n   *   image: base64EncodedImage,\n   *   prompt: \"cinematic camera movement\",\n   *   fps: 24,\n   * });\n   * ```\n   */\n  client.generateImageToVideo = async (\n    params: ImageToVideoParams\n  ): Promise<Blob> => {\n    try {\n      const response = await client.post('/api/video/image-to-video', params, {\n        responseType: 'blob',\n        timeout: 600000, // 10 minutes for long-running generation\n      });\n\n      return response.data as Blob;\n    } catch (error) {\n      if (axios.isAxiosError(error)) {\n        throw new Error(\n          error.response?.data?.message || 'Failed to generate image-to-video'\n        );\n      }\n      throw error;\n    }\n  };\n\n  return client;\n};\n\n/**\n * Video generation API client interface.\n *\n * Extends the base API client with video generation methods.\n */\nexport interface VideoAPIClient extends AxiosInstance {\n  generateTextToVideo(params: TextToVideoParams): Promise<Blob>;\n  generateImageToVideo(params: ImageToVideoParams): Promise<Blob>;\n}\n\n/**\n * Example usage in React component:\n *\n * ```typescript\n * import { getAPIClient } from '../services/api';\n * import { getCacheService } from '../services/cache';\n *\n * const MyComponent = () => {\n *   const apiClient = getAPIClient();\n *   const cacheService = getCacheService();\n *\n *   const handleGenerateVideo = async () => {\n *     try {\n *       // Generate video\n *       const videoBlob = await apiClient.generateTextToVideo({\n *         prompt: \"A cat walking\",\n *         duration: 8,\n *         fps: 24,\n *       });\n *\n *       // Cache the video\n *       const cacheKey = `video_${Date.now()}`;\n *       const cachedUri = await cacheService.cacheVideo(cacheKey, videoBlob);\n *\n *       // Use the cached video URI\n *       setVideoUri(cachedUri);\n *     } catch (error) {\n *       console.error('Error:', error);\n *     }\n *   };\n *\n *   return (\n *     <TouchableOpacity onPress={handleGenerateVideo}>\n *       <Text>Generate Video</Text>\n *     </TouchableOpacity>\n *   );\n * };\n * ```\n */\n
